#include <iostream>
#include <fstream>
#include <string>
#include <vector>
#include <unordered_map>
#include <utility>
#include <sstream>
using namespace std;
ifstream fin("sample.txt");
vector<string> lines;

int main(){
  string s;
  while(getline(fin, s))
    line.push_back(s);
  

  while(lines.size() > 1){
    for(int i=lines.size()-1;i>=0;i--){
      stringstream ss(lines[i]);
      ss >> a
    }
  }


}
